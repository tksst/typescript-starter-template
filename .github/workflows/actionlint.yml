name: Actionlint

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/**"
  pull_request:
    paths:
      - ".github/workflows/**"

jobs:
  actionlint:
    uses: tksst/github-actions-workflows/.github/workflows/actionlint.yml@aeb112a2cc7c266043b42d72642ff62937e1ffcb
