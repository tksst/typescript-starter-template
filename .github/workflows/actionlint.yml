name: Actionlint

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/**"
  pull_request:
    paths:
      - ".github/workflows/**"

jobs:
  actionlint:
    uses: tksst/github-actions-workflows/.github/workflows/actionlint.yml@4d3055bc0622325eb8d538659de8c78957a73a2a
