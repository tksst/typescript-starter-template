name: Actionlint

on:
  workflow_dispatch:
  push:
    paths:
      - ".github/workflows/**"
  pull_request:
    paths:
      - ".github/workflows/**"

jobs:
  actionlint:
    uses: tksst/github-actions-workflows/.github/workflows/actionlint.yml@6495ac6b96f806e689d068ea293b2e820dd14104
